import Head from 'next/head';
import Card from '../Components/Card/Card';

export default function card() {
  return (
    <div>
      <Head>
        <title>Login</title>
      </Head>
      <Card />
    </div>
  );
}
